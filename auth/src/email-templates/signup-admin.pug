extends base-email

block content
  p Hi #{firstName},
  p Sign up as Admin? Submit a PATCH request to: #{url}.
  p (Website for this action not yet implemented.)
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
              tr
                td
                  a(href=`${url}`, target='_blank') SignUp as Admin
  p If you didn't want to sign up as admin, please ignore this email!
